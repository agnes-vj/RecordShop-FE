namespace RecordShop_Frontend.Model
{
    public enum ExecutionStatus
    {
        SUCCESS,
        NOT_FOUND,
        VALIDATION_ERROR,
        UNAUTHORISED,
        INTERNAL_SERVER_ERROR,
        ALREADY_EXISTS,
        ARTIST_NOT_FOUND,
        FAILURE
    }
}
